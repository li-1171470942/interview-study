# 学习手册_22_RRM无线资源管理_讲述卡.md

> **来源**：D:\l32524\projects\rrmProject\ 源码 + 文档
> **炼化日期**：2026-09-04
> **对标**：学习手册_20_项目讲述方法论（但只含RRM项目）

---

## 一、60秒电梯陈述

> "我做过3年的无线资源管理系统RRM，这是企业级WiFi网络的核心组件，负责信道的自动分配、功率调整、负载均衡。说白了就是让企业的无线网络'自己管自己'，网管不用天天盯着。
>
> **技术亮点**：我主导了AI RRM的重构，核心解决一个问题——原来所有计算都在云端，海量数据传输压力大。后来改成'设备侧计算+云端管控'的架构，适配现网98%的组网类型，计算压力分流到AC设备上，云端只负责下发配置和收集结果。
>
> **架构细节**：4个Node.js微服务（rrmcontrol/rrmserver/rrmcompute/aioptimize），用Kafka做数据上报，Redis做分布式锁，MongoDB分表存储。
>
> **战场声明**：我熟悉微服务间的异步通信、分布式锁、消息可靠性这些面试高频考点，下面的场景都是我在代码里真实解决过的。"

---

## 二、简历钩子版

以下bullet可直接写进简历，每条附带面试官会追问什么：

### 1. "主导AI RRM系统重构，实现设备侧+云端双计算模式"

**会引来的追问**：为什么要设备侧计算？云端计算有什么问题？
> "原系统所有AP数据先上传云端→云端计算→下发配置，但现网98%是AC+fitap组网，AC本身就能算，何必绕一圈？改成设备侧计算后，云端只下发开关配置，AC本地执行调优，云端压力降了90%以上。"

**弹药位置**：学习手册_21_RRM场景模式库_模式三

---

### 2. "维护4个Node.js微服务，月均处理2000+调优任务，0生产事故"

**会引来的追问**：微服务之间怎么通信的？挂了怎么办？
> "服务间走RabbitMQ异步通信，数据走Kafka上报。4个服务各有职责：rrmcontrol入口、rrmserver业务、rrmcompute计算、aioptimize AI调优。任一服务挂了只影响对应功能，不影响整体，我们有K8s健康检查和自动重启。"

**弹药位置**：学习手册_21_RRM场景模式库_模式一

---

### 3. "开发分布式RRM开关同步工具，解决跨服务配置冲突问题"

**会引来的追问**：两个服务维护同一份配置？怎么同步的？
> "AI RRM和xmlconfmgr各维护一份开关配置，数据不一致。我的方案是直连下游MongoDB扫全量，按配置状态更新到我们这边。实现时绕过了HTTP网关权限限制，加了幂等性保证。"

**弹药位置**：学习手册_21_RRM场景模式库_模式三

---

### 4. "优化Kafka消费可靠性，引入死信队列+手动offset提交机制"

**会引来的追问**：Kafka消费失败怎么办？重复消费呢？
> "消费失败不commit会自动重投，重投3次进死信队列。同时在MongoDB加唯一索引防重复消费。"

**弹药位置**：学习手册_21_RRM场景模式库_模式一

---

### 5. "实现Redis分布式锁控制定时任务并发，解决K8s多副本竞态问题"

**会引来的追问**：分布式锁怎么实现？锁过期了业务没执行完呢？
> "用set NX EX原子性获取锁，设300秒过期防止死锁。但锁住到执行之间还有竞态窗口，所以在锁内加了二次检查judgeAutoSwitch。"

**弹药位置**：学习手册_21_RRM场景模式库_模式二

---

### 6. "适配WiFi7设备，新增6GHz频段全链路支持（采集→扫描→计算→调优）"

**会引来的追问**：6GHz和5G有什么区别？改动有多大？
> "原来只支持2.4G/5G，WiFi7新增6GHz频段。需要4个微服务全链路改造：rrmcontrol新增接口适配、rrmserver扩展扫描、rrmcompute改算法、aioptimize适配调优，涉及代码量比较大。"

**弹药位置**：学习手册_19_并发场景模式库_综合

---

## 三、数字锚点表

| 数字 | 故事 |
|------|------|
| **60秒** | newApUpScheduler每60秒扫描新AP触发调优 |
| **500** | 云AP并发下发上限，控制单批次请求量 |
| **20秒** | 云AP下发总超时，熔断保护 |
| **3频段** | 2.4G/5G/6G，WiFi7新增6GHz |
| **4微服务** | rrmcontrol/rrmserver/rrmcompute/aioptimize |
| **289个** | AT环境扫描到的云AP autoSwitch记录 |
| **239/25** | 开启/关闭实时调优的AP数量比 |
| **98%** | 现网AC+fitap组网占比（决定架构选型） |
| **300秒** | 分布式锁过期时间（业务执行上限） |

---

## 四、三类必死题预演

### 必死题1：消息队列可靠性
**问题**：Kafka消息消费失败了怎么处理？
**标准回答骨架**：
```
1. 失败不commit → 自动重投（At-Least-Once）
2. 重试次数用完 → 进死信队列(DLQ)人工处理
3. 业务层加幂等 → 唯一索引防止重复处理
4. 关键数据不丢失 → 落库后再commit
```

---

### 必死题2：分布式锁原理
**问题**：Redis分布式锁怎么实现？有什么坑？
**标准回答骨架**：
```
1. 实现方式：SET key value NX EX 300（原子性）
2. 过期问题：设长一点 or 看门狗续期
3. 主从切换：可能丢锁，接受 or 用Redlock
4. 业务层兜底：检查-执行竞态，锁内二次验证
```

---

### 必死题3：架构质疑
**问题**：为什么要拆成4个微服务？不是一个服务不行吗？
**标准回答骨架**：
```
1. 拆分原因：计算密集(rrmcompute) vs IO密集(rrmcontrol) vs 独立演进(aioptimize)
2. 扩展性：各服务独立扩缩容
3. 故障隔离：一个挂了不影响其他
4. 代价：运维复杂度↑、事务一致性难、网络开销
```

---

## 五、技术债清单（项目重来会改什么）

### 债1：分布式锁无看门狗续期
**位置**：`newApUpService.js` 的分布式锁
**改法**：接入Redisson，它有看门狗自动续期机制，或者把锁粒度从shop级别细化到更小的维度

### 债2：下游DB连接信息硬编码
**位置**：`syncDistributedRrm.js`
**改法**：依赖wlanpub统一配置中心，避免环境差异导致的连接失败

### 债3：四分支同步靠人工
**位置**：acceptance/develop/release/international
**改法**：CI流水线自动检测分支差异，触发cherry-pick或通知维护者

---

## 六、印象最深的Bug（四段式）

### Bug：设备上报"write after end"错误（fd4ed739）

**现象**：
> 国际环境Kafka消费日志出现大量错误，部分调优结果状态不对

**定位**：
> 查日志发现是rrmcompute OOM后重启，Kafka消费位点丢失，设备重复上报导致状态冲突

**修复**：
> 1. 增加内存限制（5Gi→5.5Gi）
> 2. 消费端加幂等处理
> 3. 硬编码功率上限绕过设备bug（临时方案）

**反思**：
> "这个bug教会我两件事：一是消息消费要有幂等设计，不能假设消息只来一次；二是OOM不能只靠加内存，要分析是谁在用，为什么爆了——后来定位是rrmcompute的算法缓存没清理。"

---

## 七、相关手册

| 手册 | 关系 |
|------|------|
| 学习手册_19_并发场景模式库 | 通用并发/异步/锁模式，可与本项目串联 |
| 学习手册_20_项目讲述方法论 | 通用讲述方法论，本卡为RRM专项版本 |
| 学习手册_01_计算机网络 | WiFi/无线网络基础，本项目领域知识 |
| 学习手册_15_无线网络 | 更系统的无线网络知识 |

---

*本卡基于 D:\l32524\projects\rrmProject\ 文档炼化，可直接用于面试准备*
