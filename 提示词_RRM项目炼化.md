# 🧪 RRM 项目八股炼化 · 提示词

> **用途**：把任意 RRM 项目素材（代码/需求/日志/故障记录）炼化成《学习手册 19 场景模式库》+《学习手册 20 项目讲述卡》同款的**面试弹药**。
> **用法**：① 按下文"输入材料清单"准备素材 → ② 复制下方提示词，把【占位符】替换成实际信息 → ③ 粘给 AI 执行 → ④ 产出两本手册并接入学习体系。
> **对标产物**：`学习手册_19_并发场景模式库.md`、`学习手册_20_项目讲述方法论.md`（本目录已有，可作格式参考）。

---

## 一、输入材料清单（喂得越全，炼得越真）

| 优先级 | 材料 | RRM 项目示例 |
|---|---|---|
| P0 | 项目代码仓库 | `D:\l32524\projects\rrmProject\`（rrmcontrol / rrmserver / rrmcompute / aioptimize 四服务源码） |
| P0 | git 提交历史（CR 记录） | `git log --oneline`，CR ID 格式 `cr id:<12位>:描述`，是"你做了什么"的权威证据 |
| P1 | 需求/设计文档 | 6G 渐进优化需求分析、RRM 适配 WiFi7 接口变更统计、WBC IoT 2.0 设计等 |
| P1 | 线上问题/故障记录 | OOM 调优记录、国际环境 MQ bug（fd4ed739）、调优失败排查（failReason 对照表） |
| P2 | 真实日志样例 | Kafka 消息日志、调度器日志（newApUpScheduler）、设备上报日志 |
| P2 | 架构/部署信息 | 4 微服务拓扑、K8s 资源（5Gi→5.5Gi）、Mongo 集合/索引、Kafka topic |

> 没有 P0 也要有 P1——**AI 只能炼化你喂进去的真实素材，缺料宁可少写不可编造**。

---

## 二、提示词主文（复制以下全部内容，替换【】后使用）

```
# 任务：RRM 项目八股炼化（面试弹药生产线）

## 角色
你是资深后端面试教练 + 项目经验炼化专家。你服务的主人是一名有 3 年无线网管平台（RRM）后端经验的工程师，正在备战跳槽面试（目标 30-35 万年包，可面 Node.js / Java / Go / C++ 岗位）。

## 输入材料
- 项目名称：【如：Oasis RRM 无线资源管理】
- 材料路径：【代码仓库 / 需求文档 / 日志文件路径列表】
- 目标岗位画像：【如：Java 后端 / 通信厂嵌入式 / AI 平台全栈】

## 你的任务
通读材料后，把它炼化成两份"能直接背、能上战场"的面试手册，写作风格对标本工作区已有的《学习手册_19_并发场景模式库》与《学习手册_20_项目讲述方法论》。

## 硬性规则（违反即返工）
1. 【真实性铁律】只炼化材料里真实存在的内容。不得虚构功能、指标、架构；拿不准的标"待确认"。数字必须能溯源到材料（代码/日志/提交记录）。
2. 【亲力亲为分级】区分"本人主导/核心开发"与"参与协作"：前者写深（含代码形态），后者只写接口边界，不得冒充主导。
3. 【可迁移 ≠ 可冒充】场景模式、话术、方法论可通用化提炼；但不得照抄其他项目的业务细节充数。
4. 【面试视角】一切产出都要回答一个问题："面试官听到这句会追问什么？"每个亮点必须预埋追问并给答案。

## 工作流程（7 步）
1. **通读**：先摸清材料结构（源码模块/需求章节/日志类型），列出该项目真实存在的技术场景清单。
2. **挖场景**：从代码/日志中找出"代码级可讲的并发/异步/可靠性/性能场景"，每条给出：业务触发原因 + 关键代码形态（贴真实片段或忠实概括）+ 涉及的技术栈。
3. **归类**：把场景映射到通用模式库（同步返回异步下发 / 延迟防抖 / 分批限速 / 消息转同步RPC / 事件驱动状态机 / 消费治理分层兜底 / 分布式锁 / 幂等 / 降级重试 / 配置与回滚 / 性能调优），允许跨类。
4. **补知识**：每个场景补"面试官会追问的底层原理"（中间件机制/并发原语/框架原理），写清答案要点——注意结合主人技术栈（Node.js 为主、Java 为转岗向、C++ 为通信厂向）。
5. **写话术**：每个场景给 30 秒面试话术（业务因果 + 设计取舍 + 一句"坑/反思"）+ 1-3 个高频追问的标准回答。
6. **提数字**：汇总该项目的"数字锚点表"（周期/阈值/规模/耗时/容量），每个数字配一句话出处故事。
7. **成稿并自检**：按下面输出结构成稿；最后过一遍"真实性检查清单"，标注所有待确认项。

## 输出结构

### 文档 A：《学习手册_21_<项目>场景模式库.md》（对标手册 19）
- 头部：来源说明（材料清单 + 炼化日期 + 待确认项列表）
- 正文：按"模式"组织，每个模式固定模板：
  - 【一句话】它解决什么问题
  - 【业务触发】主人项目里真实的触发场景（报出真实类名/方法名/常量）
  - 【代码形态】真实代码摘录或忠实概括（标注文件位置）
  - 【知识点串】对应的底层原理与八股考点
  - 【面试话术】30 秒可脱稿版本（含一句主动认的坑/债）
  - 【高频追问】表层→原理→陷阱→开放 四层各 1-2 题 + 答案要点
- 附录：跨场景的通用认知（如锁/幂等/兜底金句）；设计模式在项目代码里的真实落点表。

### 文档 B：《学习手册_22_<项目>讲述卡.md》（对标手册 20，但只含本项目）
- 60 秒电梯陈述（业务→架构一句话→2 个亮点→战场声明）
- 简历"钩子版"：可写进简历的 4-6 条 bullet，每条附【会引来的追问 → 你的主场 → 弹药位置】
- 数字锚点表（含出处故事）
- 三类必死题预演（防重入/消息可靠性/架构质疑）+ 标准回答骨架
- 项目重来一次会改什么（3 条技术债，每条报位置+改法）
- 印象最深的 bug 四段式（现象→定位→修复→反思）

### 文档 C：接入学习体系（同目录操作）
1. md 成稿后转 HTML（若环境无工具则说明转换步骤）；
2. 把两本挂入 `学习中心.html`（手册区卡片，编号顺延）+ 更新 `学习手册索引.md` 与 `求职复习进度中枢.md` 的文件清单；
3. 若与现有手册 01-20 有内容重叠，在头部注明"相关手册：XX"，不重复展开。

## 交付格式
- 直接在对话输出两份手册全文（便于检查），再落盘为 md 文件。
- 结尾给一段 200 字内的"炼化摘要"：项目有哪些可讲亮点、哪些待确认、建议优先背哪节。
```

---

## 三、针对 RRM 的已知弹药提示（可选喂给 AI 的提示，帮助它问对问题）

以下是你 RRM 项目的已知线索，可随提示词一起附上，让 AI 按图索骥：

- 微服务：rrmcontrol（调度/消息）/ rrmserver（业务）/ rrmcompute（计算，OOM 5Gi→5.5Gi）/ aioptimize（AI 优化）
- 架构：K8s + Kafka + MongoDB + Redis（腾讯云 10.11.x / 国际云 10.0.x 双环境），云 AP / AC+fitap 组网
- 关键机制：newApUpScheduler 每 60s 扫新 AP 触发调优；optType=25(AP上线) 消息由 wlan_apcomp_lb 产生；渐进优化 scheduler 触发无白名单；老 AI 优化有环境白名单
- 调度与消息：MongoDB 集合 clbDetail/optHistory/autoSwitch/autoOptConfig，索引 {taskId, apSN, RI}
- 下发链路：WebSocket + netconf 到 AC；设备上报经 Kafka 回传；'write after end' bug（fd4ed739，硬编码功率上限）
- 多分支发版：acceptance/international/release/develop 四分支同步 + 备份 tag

---

## 四、产出验收清单（炼化完对着查）

- [ ] 每个亮点都能在材料里找到出处（代码位置/日志/提交号）
- [ ] 场景 ≥ 4 个，且覆盖"并发正确性/消息可靠性/性能/架构取舍"至少两类
- [ ] 每个场景有 30 秒话术 + 至少 1 个陷阱级追问的诚实回答（主动认坑）
- [ ] 数字锚点 ≥ 6 个，全部可溯源
- [ ] 电梯陈述脱稿能讲 60 秒不卡
- [ ] 无虚构指标/功能；待确认项已显式标注
- [ ] 已接入学习中心/索引，可与手册 19/20 串联学习
