# 云简平台架构分析 — 设备上报路由与微服务通信机制

> 分析日期：2026-09-09
> 分析方式：本地代码挖掘（rrmcontrol/wlanpub/rrmProject）+ 北京 RD 环境（117.128.51.40:21091）K8s 实测验证
> 用途：面试"讲平台架构"弹药 + 理解自己所在系统全貌

---

## 一、挖掘思路（方法论，可复用）

要搞清一个平台的架构，按 4 条链路追：

| 链路 | 关键问题 | 挖哪里 |
|------|---------|--------|
| ① 设备→云端 | 设备上报怎么进到指定微服务 | 设备接入网关（emqtt/cloudagent/websocket）→ MQ 分发 |
| ② 外部 HTTP→微服务 | 浏览器/APP 请求怎么路由 | 统一入口（Kong/nginx/v2webserver）→ 按 path 转发 |
| ③ 内部服务→服务 | 微服务间怎么调 | RabbitMQ routing key（wlanpub mqoper）|
| ④ MQ 消息路由 | 消息怎么到"对的消费者" | topic 注册表（Kafka dispatcher）/ exchange 绑定 |

**速查命令**：
```bash
kubectl get deploy | grep -iE "gateway|web|nginx|emqtt|cloudagent"   # 网关/接入层
kubectl get svc | grep -iE "NodePort|LoadBalancer"                    # 外部入口
kubectl get deploy <name> -o yaml | grep -A5 envFrom                  # 配置来源
kubectl exec <pod> -- cat /etc/nginx/nginx.conf                       # nginx 路由表
```

---

## 二、云简平台总体架构（RD 实测）

### 2.1 外部 HTTP 请求入口链

RD 环境实测到的入口服务：

| 服务 | 类型 | 端口 | 职责推断 |
|------|------|------|---------|
| **Kong (kong-deploy/kong-proxy)** | API 网关 | NodePort 18000/48443 | 外部流量总入口（HTTPS→HTTP 转换、限流、路由）|
| **oasis-v2webserver** | Web 服务 | NodePort 8888 | 老版 WEB 聚合层，页面→后端接口转发 |
| **oasis-rest-gateway** | REST 网关 | NodePort 8899 | REST API 统一网关（新版）|
| **oasis-gateway** | 网关 | 自研镜像 | 老网关/设备配置网关 |
| **oasis-swagger-gateway** | Swagger | ClusterIP | 接口文档聚合 |

> 路由规律推测：**Kong（总入口）→ 按域名/路径 → v2webserver / rest-gateway → 业务微服务**。业务侧用 `OASIS_ENV=development` 等环境变量区分分支（acceptance/release/international）。

### 2.2 设备接入链（设备上报的核心！）

RD 实测到 **EMQ X (emqtt v3.2.3)** 消息服务器集群：

| 服务 | 协议/端口 | 说明 |
|------|----------|------|
| emqtt | MQTT 1883 | 设备消息接入（TCP）|
| emqttssl | MQTT 8883 + 18083 | **设备安全接入**（SSL + Dashboard）|
| emqtthttp / emqttny | 1883 变体 | HTTP/内网接入 |
| cloudagent | NodePort 27080/27443 | 设备代理/云 AP 通道 |

**设备上报路径（推断，结合代码）**：
```
AP/AC 设备 --MQTT/长连接--> EMQ X (emqtt)
        --> xmlconfmgr / cloudagent（接入处理）
        --> Kafka topic（如 optType 25/45/46 号消息）
        --> 各微服务按 topic 注册表消费（rrmcontrol/rrmserver/rrmcompute/aioptimize）
```

### 2.3 内部微服务通信（代码实证）

**wlanpub 公共库（mqoper.js）— 全平台 MQ 底座**：
- Exchange：`ex_direct` / `ex_reply` / `ex_broadcastrequest` / `ex_multiReq` / `ex_wsbroadcastrequest`
- 广播 key：`broadcast_key` / `broadcast_webserver_key`
- 单播：`mqhd.sendMsg(pkey, msg)`；组播：`mqhd.multicast(routingKey, msg)`
- 自动重连 + 10 分钟 10 次错误自动重启保护

**routing key 格式（rrmcontrol/send2micro.js 实证）**：
```
rrm.rrmcompute.rrmcontrol   = 业务.目标服务.源服务
rrm.rrmserver.rrmcontrol
sample.rrmcompute.rrmcontrol
```

**微服务注册订阅**（rrmcontrol.js 实证）：
```js
mqhd.connectReadyForMicroService("rrmcontrol", procMqMod.recvMqMsg, ...)
```

**Kafka 消息分发（dispatcher.js 实证）**——"消息如何到达指定微服务"：
```js
kafkaProcMapping[topic] = callback   // topic → 处理函数 注册表
procKafkaMsg: topic 查表 → 调对应 procFunction
kafkaInit(serviceName, topicMapping) // 每个微服务只订阅自己关心的 topic
```

---

## 三、设备下发链路（云端 → 设备）完整拆解

**cmdproxy.js（wlanpub/lib）实证**——`sendCmd2Device(devSN, jsonData)`：

```
1. Redis 查 conn_info:{devSN}/{cloudModName}
   → 拿到设备当前所在接入节点的 serviceAddress（关键！设备连接是分布式的）
2. 生成全局 seqNumber（redlock.getGlobalSeqNumber）
3. Redis 存 CONTEXT:SEQUENCENUMBER:{seq}（请求上下文 + 回调）
4. mqhd.sendMsg(socketAddress, msg) → 发到该设备所在接入节点
5. 60s 超时（可配 cfgTimeout），等设备响应 → 回调 map 触发
```

**应答机制**：`option.exchange = exResponse`（reply exchange）+ messageId + Redis 回调 map —— **同步等待设备应答的异步实现**（面试亮点：这是"消息转同步 RPC"模式）。

---

## 四、技术栈全景（RD 实测汇总）

| 层 | 技术 | 备注 |
|----|------|------|
| 外部入口 | **Kong API 网关** | NodePort 18000/48443 |
| Web 聚合 | oasis-v2webserver（Java + SkyWalking）| envFrom: env-config configmap |
| 设备接入 | **EMQ X MQTT**（v3.2.3）| 1883/8883/18083 多端口集群 |
| 设备代理 | cloudagent / xmlconfmgr | 云 AP 通道 |
| 服务通信 | **RabbitMQ**（wlanpub mqoper）| ex_direct/ex_reply/ex_broadcast |
| 数据管道 | **Kafka** | topic 注册表分发（kafka40/41/44 hostAliases）|
| 配置下发 | netconf/WebSocket | rrmcontrol 设备指令 |
| 存储 | MongoDB + Redis | Redis 存设备连接信息/分布式锁 |
| 部署 | K8s + NodePort | hostPath 日志挂载 /home/azureuser/logs |
| 可观测 | SkyWalking + 自研 monitor | Java agent initContainer |

---

## 五、"设备上报消息如何到达指定微服务" — 一句话版（面试可用）

> "设备走 **MQTT 连到 EMQ X 接入集群**，接入服务把消息转成 **Kafka 事件**（按 optType 区分，如 AP 上线 25 号、调优结果 45/46 号）。**每个微服务启动时注册自己关心的 topic**（Kafka dispatcher 的 topic→回调注册表），消息来了按 topic 查表分发到对应处理函数；微服务之间的同步调用走 **RabbitMQ**，routing key 格式是`业务.目标服务.源服务`，设备命令下发则先查 Redis 里设备所在节点，再路由过去。"

---

## 六、待深入确认项

- [ ] Kong 的路由表具体配置（存 PostgreSQL，需连 kong-database 查）
- [ ] emqtt 到 Kafka 的转换服务是哪个（xmlconfmgr？websocket？待查）
- [ ] optType 消息分类全表（25/45/46/109 等各自含义）
- [ ] v2webserver vs rest-gateway 的分工边界（新老版本并存？）
- [ ] 设备连接信息 conn_info 由谁写入 Redis（接入层心跳上报？）
